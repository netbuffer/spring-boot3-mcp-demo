server:
  port: 8081

spring:
  application:
    name: mcp-client
  ai:
    chat:
      client:
        enabled: true
    deepseek:
      api-key: ${DEEPSEEK_API_KEY}
      base-url: https://api.deepseek.com

logging:
  level:
    root: INFO
    cn.netbuffer: DEBUG